#include "main.h"

/**
 * _abs - The prints the absolute value of an integer
 *
 * @x: integer
 *
 * Return: the abslute value
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (x * -1);
	}
}
